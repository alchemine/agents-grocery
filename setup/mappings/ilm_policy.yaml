log_policy:
  policy:
    phases:
      hot:
        actions:
          rollover:
            max_size: "1gb"
      delete:
        min_age: "0ms"
        actions:
          delete: {}

inference_cache_policy:
  policy:
    phases:
      hot:
        actions:
          rollover:
            max_size: "1gb"
      delete:
        min_age: "0ms"
        actions:
          delete: {}
